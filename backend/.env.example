# Configurações do Sistema NFe - Exemplo
# Copie este arquivo para .env e configure suas variáveis

# ==================== AMBIENTE NFe ====================
# NODE_ENV: development | production
NODE_ENV=production

# AMBIENTE NFe: 1=Produção | 2=Homologação
# 1 = PRODUÇÃO (NFe real, válida fiscalmente)
# 2 = HOMOLOGAÇÃO (NFe para testes e credenciamento)
AMBIENTE=2

# SIMULATION_MODE: true=simulação | false=real
# Para produção, sempre usar false
SIMULATION_MODE=false

# DEBUG_MODE: true=logs detalhados | false=logs normais
DEBUG_MODE=false

# Servidor
PORT=3000
HOST=localhost

# Segurança
JWT_SECRET=seu_jwt_secret_aqui
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Banco de dados - Sistema 100% JSON
USE_MONGODB=false
DATABASE_TYPE=json

# ==================== CONFIGURAÇÕES NFe ====================
# UF do emitente (SP, RJ, MG, etc.)
UF=SP

# CNPJ do emitente (apenas números)
CNPJ_EMITENTE=seu_cnpj_aqui

# Certificado Digital (opcional - cliente importa via interface)
CERTIFICADO_PATH=
CERTIFICADO_PASSWORD=

# Configurações SEFAZ
SEFAZ_TIMEOUT=30000
SEFAZ_RETRY_ATTEMPTS=3

# Numeração NFe
SERIE_NFE=1
NUMERO_INICIAL_NFE=1

# Logs
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# Admin principal
SEED_ADMIN_NOME=Administrador
SEED_ADMIN_EMAIL=admin@suaempresa.com.br
SEED_ADMIN_SENHA=sua_senha_aqui
ENABLE_AUTO_SEED=true

# CORS - Configure seus domínios
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Monitoramento
HEALTH_CHECK_ENABLED=true
METRICS_ENABLED=false

# Cache
CACHE_TTL=300