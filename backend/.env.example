# ==================== CONFIGURAÇÕES DO SERVIDOR ====================

# Porta do servidor (padrão: 3001)
PORT=3001

# Ambiente de execução (development, production, test)
NODE_ENV=development

# ==================== CONFIGURAÇÕES DE AUTENTICAÇÃO ====================

# Chave secreta para JWT (ALTERE EM PRODUÇÃO!)
JWT_SECRET=sua-chave-secreta-jwt-muito-forte-aqui

# Tempo de expiração do token JWT (ex: 24h, 7d, 30m)
JWT_EXPIRES_IN=24h

# API Keys válidas (separadas por vírgula)
API_KEYS=nfe-dev-key-123456,nfe-prod-key-abcdef

# ==================== CONFIGURAÇÕES NFE/SEFAZ ====================

# Ambiente SEFAZ (1=Produção, 2=Homologação)
AMBIENTE=2

# UF do emitente (código IBGE - ex: 35 para SP, 50 para MS)
UF=MS

# CNPJ do emitente (apenas números)
CNPJ_EMITENTE=

# ==================== CERTIFICADO DIGITAL ====================

# Caminho para o arquivo do certificado (.pfx ou .p12)
CERT_PATH=

# Senha do certificado digital
CERT_PASS=1234

# ==================== CONFIGURAÇÕES DE LOGS ====================

# Diretório para armazenar logs (padrão: ./logs)
LOGS_DIR=./logs

# Dias para manter logs antigos (padrão: 30)
LOGS_RETENTION_DAYS=30

# Nível de log (error, warn, info, debug)
LOG_LEVEL=info

# ==================== CONFIGURAÇÕES DE REDE ====================

# Timeout para requisições SEFAZ (em milissegundos)
TIMEOUT=30000

# Número máximo de tentativas para requisições SEFAZ
RETRY_ATTEMPTS=3

# Manter conexão ativa
KEEP_ALIVE=true

# ==================== CONFIGURAÇÕES DE SEGURANÇA ====================

# Rate limiting - máximo de requests por janela de tempo
RATE_LIMIT_MAX=200

# Rate limiting - janela de tempo em milissegundos (15 minutos = 900000)
RATE_LIMIT_WINDOW=900000

# CORS - origens permitidas (separadas por vírgula)
CORS_ORIGINS=https://brandaocontador.com.br,https://app.brandaocontador.com.br,http://localhost:3000

# ==================== CONFIGURAÇÕES ESPECÍFICAS NFE ====================

# Série padrão das NFes
NFE_SERIE_PADRAO=1

# Número inicial das NFes (se não houver banco de dados)
NFE_NUMERO_INICIAL=1

# ==================== CONFIGURAÇÕES DE DESENVOLVIMENTO ====================

# Habilitar logs detalhados (true/false)
DEBUG_MODE=true

# Habilitar modo de simulação (não envia para SEFAZ real)
SIMULATION_MODE=true

# Salvar XMLs gerados em arquivo (true/false)
SAVE_XML_FILES=true

# Diretório para salvar XMLs
XML_OUTPUT_DIR=./xmls

# ==================== OBSERVAÇÕES ====================

# 1. NUNCA commite este arquivo com valores reais em produção
# 2. Copie este arquivo para .env e preencha com os valores corretos
# 3. O arquivo .env deve estar no .gitignore
# 4. Em produção, use variáveis de ambiente do sistema ou serviços seguros
# 5. Todas as senhas e chaves devem ser fortes e únicas
# 6. O certificado digital deve estar em local seguro e com permissões restritas