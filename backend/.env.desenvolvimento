# Configurações de DESENVOLVIMENTO (Simulação)
PORT=3001
NODE_ENV=development
DEBUG_MODE=true

# Autenticação
JWT_SECRET=brandaocontador-nfe-secret-key-2024
JWT_EXPIRES_IN=24h

# NFe/SEFAZ - Homologação em modo simulado
AMBIENTE=2
UF=MS
CNPJ_EMITENTE=

# Certificado (não utilizado em simulação)
SIMULATION_MODE=true
CERT_PATH=
CERT_PASS=

# Logs
LOGS_DIR=./logs
LOG_LEVEL=debug

# Rede
TIMEOUT=30000
RETRY_ATTEMPTS=1
KEEP_ALIVE=true

# Segurança
RATE_LIMIT_MAX=500
RATE_LIMIT_WINDOW=900000
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# NFe específicas
NFE_SERIE_PADRAO=1
NFE_NUMERO_INICIAL=1

# Arquivos XML
SAVE_XML_FILES=true
XML_OUTPUT_DIR=./xmls

# Seed do usuário administrador (para desenvolvimento)
SEED_ADMIN_NOME=Administrador
SEED_ADMIN_EMAIL=admin@brandaocontador.com.br
SEED_ADMIN_SENHA=admin123